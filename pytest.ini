[pytest]
testpaths = backend/tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
# Legacy Azure-era tests (root + the old agent test) import removed dependencies.
addopts = --ignore=backend/tests/test_agent.py
norecursedirs = .venv frontend node_modules chroma_data chromadb_data
